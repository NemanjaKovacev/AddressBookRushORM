<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nemanja.addressbookrushorm">

    <application
        android:name=".MyRushApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- DATABASE METADATA -->
        <meta-data
            android:name="Rush_classes_package"
            android:value="com.example.nemanja.addressbookrushorm.models" />
        <meta-data
            android:name="Rush_db_version"
            android:value="1" />
        <meta-data
            android:name="Rush_db_name"
            android:value="ContactsDB.db" />
        <meta-data
            android:name="Rush_debug"
            android:value="false" />
        <meta-data
            android:name="Rush_requires_table_annotation"
            android:value="false" />
        <meta-data
            android:name="Rush_log"
            android:value="false" />

    </application>
</manifest>